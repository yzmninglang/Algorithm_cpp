58题

对于需要知道最后一个单词的长度，我们只需要从后面往前面遍历，一步一步来，直到遇到第一个空格为止。

初始化curr指针为长度减去1,

开始循环，对于后面是空格结尾的，先去空格，使用`curr>=0 && s[curr]==' '`，然后curr自我相减

对于不是以空格结尾的，则将字符传入到一个新的str中，直到遇到第一个空格

返回收集起来的字符的长度



66题

对于一个数组，需要知道这个数组的长度l，设置起始值为l-1，设置一个result

for 循环，每次循环乘以(10^l-i)，加入result

返回result+1





67题

对于传入的str，我需要想将其转化为int（十进制）（stoi），计算，

再转化为二进制,在转化为str

直接使用求2取余数

---

67不能使用原来的思维，因为这个字符串可能很大

> 数字转char: 数字+'0'
>
> char转数字：char-'0'

- 设置i为a的末尾，j为b的末尾，进位信号为0

如果i不为队头，j不为队头，进位信号不为0，则往前继续

- 设置sum = carray
- 如果i,j OK，则sum = sum+ str[i]+str[j]，i和j需要自减
- 结果res.push((sum%2)+'0');
- 新的carry = sum%2



---



69题

需要注意int的范围



---

83题

设置一个ListNode* tmp，next, pre，设置一个记录上一个值的

遍历整个ListNode

直到tmp不为空

- 如果tmp的值和pre一个值相等
  - next =tmp.next
  - pre.next  = next
  - delete tmp;
  - tmp = next
- 其他

---

88题

> 结果存在nums1中

初始化一个i 为数组1的起始指针，初始化一个j为数组2的起始指针；

while遍历（i或者j不是数组的end）

第i个元素大于第j个元素：那就将这个值插入在第i-1这个位置

如果第i个元素小于第j元素：那么i++

end

如果j不是数组的end，那么将j之后的数字都插入i的最后一位；

如果i不是数组的end，那么继续往后走，如果有0 ，则pop掉

---



101题：树对称问题

这个判断树是否对称，你需要将左边数的左边和右边数的右边对比，左边数的右边和右边数的左边对比

关心最后回退的情况

如果左右子树均为空→true

如果左右子树一边为空、或两者子不相等→false

返回左树→左，右树→右；左树→右，右树→左

---

104 二叉树的深度

visTree

如果是最后一层，深度为1

问左树深度，问右树深度

返回左右树最大深度+1

---

110 平衡二叉树问题

二叉树的定义：任何一个root的左右子树的高度差不超过一：

int count(root)

如果为空指针→返回0

其他情况下，如果左右相差2以上，返回-1

如果左节点返回-1或者右节点返回-1，返回-1

都不满足，返回左右子树最大值+1

---

112 路径总和

>  叶子节点：左右子树均为空

如果为nullptr，返回false，因为这代表着叶子节点或者说一边节点，但是如何target为0不会传递下来

如果root不为nullptr，target← target-this->val

如果左右子树均为空，且target为0，返回true；

如果左右子树为空，返回false

其他情况返回（左路径判断||右路径判断）